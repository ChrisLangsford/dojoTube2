<% if user_signed_in? %>
	<li class="dropdown">
		<a href="" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false">			
		<%= image_tag avatar_url(current_user.email, 35), id: "menu-avatar"%> 
		</a>
		<ul class="dropdown-menu" role="menu">
        	<li class="dropdown-header"> Logged in: <b><%=current_user.username? ?  current_user.username : current_user.email %></b></li>
        	<li class="divider"></li>
        <%if current_user.present? && (current_user.role == "Super user" || current_user.role == "Admin")%>
        	<li><%=link_to "My Videos",my_videos_path %></li>
        	<li class="divider"></li>
        <%end%>
		<%if current_user.present? && current_user.role == "Super user"%>
        	<li class="dropdown-header"> Admin Tasks</li>
        	<li><%= link_to "Manage Users", user_index_path%></li>                              
            <li><%= link_to "Manage Dojos", dojos_path%></li> 
            <li><%= link_to "Manage Genres", genres_path%></li>
            <li class="divider"></li>                  
        <%end%>
			<li><%= link_to "Update Account", edit_user_registration_path%></li>
			<li><a href="#"></a></li>
			<li class="divider"></li>
			<li><%= link_to('Logout', destroy_user_session_path, method: :delete) %></li>
		</ul>
	</li>
<% else %>
	<li><%= link_to('Login', new_user_session_path)  %></li>
<% end %>